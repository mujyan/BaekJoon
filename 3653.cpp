#include<iostream>
using namespace std;

int tree[400001];
int movie[100001];
int where[100001];
int create(int start, int end, int node) {
    if(start == end) {
        if()
        return tree[node] = where[start];
    }
    int mid = (start + end) / 2;
    create()
}

int main() {
    int T, N, M, i;
    cin >> T;
    for(;T>0;T--) {
        cin >> N >> M;
        for(i=0; i<N; i++) {
            where[i] = N-i;
        }

    }
}